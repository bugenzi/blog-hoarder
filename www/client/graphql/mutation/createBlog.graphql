mutation CreateBlogs($input: BlogInput!) {
  postBlog(input: $input) {
    blog {
      link
      title
      text
      id
      createdAt
      authorId
    }
    errors {
      field
      message
    }
  }
}
